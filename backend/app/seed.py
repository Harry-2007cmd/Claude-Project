"""Dummy data seeder — placeholder stub created in Phase 0 (T0.1).

Built for real in T2.2: runs on startup and populates users/posts/comments/
rides only if the DB is empty, so the owner never does manual DB setup.
"""
