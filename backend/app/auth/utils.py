"""Password hashing + JWT create/verify — placeholder stub created in Phase 0 (T0.1).

Built for real in T2.3: passlib bcrypt hashing and python-jose JWT helpers,
plus the get_current_user dependency. JWT_SECRET_KEY comes from backend/.env.
"""
